test exits.txt && echo "path exists"
test -f exits.txt && echo "is a file"
test -d exits.txt && echo "is a directory"
test -d precedence_test.sh && echo "is a directory"
echo -e " starting [] "
[ exits.txt ] && echo "path exists"
[ -f exits.txt ] && echo "is a file"
[ -d exits.txt ] && echo "is a directory"
[ -d precedence_test.sh ] && echo "is a directory"
